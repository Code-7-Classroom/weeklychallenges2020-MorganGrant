//Level 4: Hard
// HARD: Create 4 math functions, one called add() that adds 2 numbers, one called 
//  subtract() that subtracts 2 numbers, one called multiply() to multiply 2 numbers, 
//  and one called divide() to divide two numbers.
 

function add(number, number2) { return (number + number2) };

function subtract(number, number2) { return number - number2 };

function multiply(number, number2) { return number * number2 };

function divide(number, number2) { return number / number2 };

// var addFunction = add(10, 5);
// var subtractFunction = subtract(10, 5);
// var multiplyFunction = multiply(10, 5);
// var divideFunction = divide(10, 5);

// console.log( addFunction, subtractFunction, multiplyFunction,divideFunction);